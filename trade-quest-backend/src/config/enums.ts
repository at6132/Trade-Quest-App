export enum AuthProvider {
  GOOGLE = 'google',
  EMAIL = 'email',
}

export enum AssetType {
  IMAGE = 'image',
  AVATAR = 'avatar',
  DOCUMENT = 'document',
  VIDEO = 'video',
  AUDIO = 'audio',
  OTHER = 'other',
}

export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum TwoFactorMethod {
  EMAIL = 'email',
  SMS = 'sms',
  AUTHENTICATOR = 'authenticator',
}
